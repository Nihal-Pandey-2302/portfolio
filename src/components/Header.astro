---
import { SITE_CONFIG } from "@config";
---

<header class="site-header">
  <div class="container flex justify-between items-center" style="max-width: 100vw;">
    <a href="/" class="logo mono">
      <span class="text-accent">&lt;</span>{SITE_CONFIG.author} <span class="text-accent">/&gt;</span>
    </a>
    
    <!-- Desktop Nav -->
    <nav class="desktop-nav">
      <ul class="flex gap-sm">
        {
          SITE_CONFIG.navLinks.map((link) => (
            <li>
              <a href={link.href} class="nav-link mono">
                {link.text}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>

    <!-- Mobile Menu Button -->
    <button id="mobile-menu-btn" class="mobile-menu-btn" aria-label="Toggle Menu" aria-controls="mobile-nav" aria-expanded="false">
      <span class="bar"></span>
      <span class="bar"></span>
      <span class="bar"></span>
    </button>
  </div>

  <!-- Mobile Nav Overlay -->
  <nav id="mobile-nav" class="mobile-nav">
    <ul class="flex flex-col items-center gap-sm">
      {
        SITE_CONFIG.navLinks.map((link) => (
          <li>
            <a href={link.href} class="mobile-nav-link mono">
              {link.text}
            </a>
          </li>
        ))
      }
    </ul>
  </nav>
</header>

<script>
  const menuBtn = document.getElementById('mobile-menu-btn');
  const mobileNav = document.getElementById('mobile-nav');
  const mobileLinks = document.querySelectorAll('.mobile-nav-link');

  if (menuBtn && mobileNav) {
    function onEscape(e: KeyboardEvent) {
      if (e.key === 'Escape') {
        closeMenu();
        menuBtn!.focus();
      }
    }

    function closeMenu() {
      menuBtn!.classList.remove('active');
      mobileNav!.classList.remove('active');
      document.body.classList.remove('no-scroll');
      menuBtn!.setAttribute('aria-expanded', 'false');
      document.removeEventListener('keydown', onEscape);
    }

    menuBtn.addEventListener('click', () => {
      const open = menuBtn.classList.toggle('active');
      mobileNav.classList.toggle('active', open);
      document.body.classList.toggle('no-scroll', open);
      menuBtn.setAttribute('aria-expanded', String(open));
      if (open) document.addEventListener('keydown', onEscape);
      else document.removeEventListener('keydown', onEscape);
    });

    // Close menu when a link is clicked
    mobileLinks.forEach(link => {
      link.addEventListener('click', closeMenu);
    });

    // Close if the viewport grows past the mobile breakpoint while open
    // (the hamburger disappears at >768px, which would trap the overlay)
    matchMedia('(min-width: 769px)').addEventListener('change', (e) => {
      if (e.matches) closeMenu();
    });
  }
</script>

<style>
  .site-header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: var(--header-height);
    background: rgba(10, 10, 12, 0.9);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    z-index: 1000;
    display: flex;
    align-items: center;
  }

  .logo {
    font-size: 1.2rem;
    font-weight: 700;
    letter-spacing: -0.02em;
    z-index: 1001; /* Above mobile menu */
  }

  .nav-link {
    font-size: 0.9rem;
    padding: 0.5rem 1rem;
    border-radius: 4px;
    transition: all 0.2s ease;
  }

  .nav-link:hover {
    color: var(--color-accent);
    background: rgba(255, 255, 255, 0.05);
  }

  /* Mobile Menu Styles */
  .mobile-menu-btn {
    display: none;
    flex-direction: column;
    justify-content: space-between;
    width: 30px;
    height: 21px;
    background: transparent;
    border: none;
    cursor: pointer;
    z-index: 1001;
    padding: 0;
  }

  .bar {
    width: 100%;
    height: 3px;
    background-color: var(--color-text-main);
    border-radius: 3px;
    transition: all 0.3s ease;
  }

  .mobile-nav {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    background: var(--color-bg);
    display: flex;
    justify-content: center;
    align-items: center;
    transform: translateX(100%);
    /* visibility keeps the closed menu out of the tab order and a11y tree */
    visibility: hidden;
    transition: transform 0.3s ease-in-out, visibility 0.3s;
    z-index: 1000;
  }

  .mobile-nav.active {
    transform: translateX(0);
    visibility: visible;
  }

  .mobile-nav-link {
    font-size: 1.5rem;
    color: var(--color-text-main);
    padding: 1rem;
    display: block;
  }

  .mobile-nav-link:hover {
    color: var(--color-accent);
  }

  /* Hamburger Animation */
  .mobile-menu-btn.active .bar:nth-child(1) {
    transform: translateY(9px) rotate(45deg);
  }

  .mobile-menu-btn.active .bar:nth-child(2) {
    opacity: 0;
  }

  .mobile-menu-btn.active .bar:nth-child(3) {
    transform: translateY(-9px) rotate(-45deg);
  }

  :global(body.no-scroll) {
    overflow: hidden;
  }

  @media (max-width: 768px) {
    .desktop-nav {
      display: none;
    }

    .mobile-menu-btn {
      display: flex;
    }
  }
</style>
